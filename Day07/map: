#include <bits/stdc++.h>
using namespace std;
const int N=50020;
map<string,int>mp;
string str[N];
int main()
{
    int cnt=0;
    while(cin>>str[cnt])
        mp[str[cnt++]]=1;
    for(int i=0; i<cnt; i++)
        for(int j=1; j<str[i].length()-1; j++)
        {
            string s1(str[i],0,j);
            string s2(str[i],j);
            if(mp[s1]&&mp[s2])
            {
                cout<<str[i]<<endl;
                break;
            }
        }
    return 0;
}
